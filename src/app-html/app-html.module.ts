import { NgModule } from '@angular/core';
import { BrowserModule } from '@angular/platform-browser';

import { AppHtmlComponent } from './app-html.component';

@NgModule({
  declarations: [
    AppHtmlComponent
  ],
  imports: [
    BrowserModule
  ],
  providers: [],
  bootstrap: [AppHtmlComponent]
})
export class AppHtmlModule { }
